# Dev image for future infrastructure compose (hot reload via air).
FROM golang:1.25-bookworm

WORKDIR /app

RUN go install github.com/air-verse/air@latest

COPY go.mod go.sum ./
RUN go mod download

COPY . .

EXPOSE 1323

CMD ["air", "-c", ".air.toml"]
